import React from "react";
export const PeopleList = props => {
    return (
      <div>
         {/* props.pep.map((first_name) => ( */}
            <div>{this.props.pep}</div>
      </div>
    );
  };
