// returns the person clicked via matching ID number
// export const FriendsButton = idNum => {
//   return (
//     <div>
//       {people.filter(person => (person.id === idNum)).friends.map(friend => (
//         <div>{Object.values(friend).join(' ')}</div>
//       ))}
//     </div>
//   )
// }
